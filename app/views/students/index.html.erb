<br />
<div class="jumbotron text-center">
  <h2>Students</h2> 
</div>
<br />

<div class="container">
    <div class="row">
        <% @students.each do |student| %>
            <%= render "student_resume", student: student %>
        <% end %>
    </div>
</div>