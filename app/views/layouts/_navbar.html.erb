<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="#">DCLITS</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item active">
                <a class="nav-link" href="#">Home</a>
            </li>
            <% if teacher_logged_in? %>
                <li class="nav-item">
                    <%= link_to "Admin Teacher", admin_teachers_url, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Problems Available", problems_url, class: "nav-link" %>
                </li>
                <li class="nav-item">
                    <%= link_to "Students", students_url, class: "nav-link" %>
                </li>
            <% end %>
            <% if student_logged_in? %>
                <li li class="nav-item">
                    <%= link_to "Tareas", "/tasks/index", method: "get", class: "nav-link" %>
                </li>
            <% end %>
            <% if logged_in? %>
                <li li class="nav-item">
                    <%= link_to "Log out", logout_path, method: "delete", class: "nav-link" %>
                </li>
            <% end %>
        </ul>
    </div>
</nav>