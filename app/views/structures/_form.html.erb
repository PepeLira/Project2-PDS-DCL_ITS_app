<%= form_with(model: structure) do |form| %>
  <% if structure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(structure.errors.count, "error") %> prohibited this structure from being saved:</h2>

      <ul>
        <% structure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :admin_teacher_id, style: "display: block" %>
    <%= form.number_field :admin_teacher_id %>
  </div>

  <div>
    <%= form.label :moment_reference, style: "display: block" %>
    <%= form.text_field :moment_reference %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
