json.extract! problem, :id, :admin_teacher_id, :structure_id, :title, :description, :extern_joins_points, :forces_moments_points, :con_rod_points, :ball_joint_points, :created_at, :updated_at
json.url problem_url(problem, format: :json)
